settings:
    spawn-radius: 16
    update-folder: update
aliases:
    icanhasbukkit:
    - version
database:
    username: bukkit
    isolation: SERIALIZABLE
    driver: org.sqlite.JDBC
    password: walrus
    url: jdbc:sqlite:{DIR}{NAME}.db
